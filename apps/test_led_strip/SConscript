Import('env', 'libs')

env = env.Clone()
deps = ['neopixel', 'arduino']
env.Append(LIBS=[libs[dep] for dep in deps])

obj = env.StaticObject('test_led_strip.cpp')
hex_ = env.HexObject(obj)
Return('hex_')
